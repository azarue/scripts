
sudo yum install gcc gcc-c++ wget

wget https://nodejs.org/dist/latest/node-v10.1.0.tar.gz

tar xzvf node-v10.1.0.tar.gz

./configure

make

sudo make install

node --version

# firewall-cmd --zone=public --add-port=80/tcp --permanent
# firewall-cmd --reload
# firewall-cmd --permanent --zone=public --list-all

# setsebool -P ftpd_full_access 1
# SELINUX=disabled
